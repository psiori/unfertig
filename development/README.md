# Retired development data

`data.json` is a retirement marker that makes old explicit data paths fail rather
than initialize a second board. Historical records remain in Git. Consult the
owning host's documentation for its current board location. Test feature branches
on disposable data and a separate port.
